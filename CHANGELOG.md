# 更新日志

本文件记录面向用户和贡献者的重要变化。版本遵循语义化版本的意图；在 1.0.0 前，MVP 的行为和界面仍可能调整。

## [0.1.0] - 2026-08-08

### 新增

- macOS 原生 SwiftUI 工作流：选择 GPX、照片目录、时区和相机时钟偏移。
- GPX 1.0/1.1 流式解析、轨迹规范化和可解释的匹配分类。
- 可靠插值、停留候选批量确认、缺轨保护、最近点和多轨冲突处理。
- ExifTool 13.59 随应用分发，写入 Lightroom 可读取的 XMP GPS 字段。
- RAW 只读、XMP 原子事务、幂等预检、已有 GPS 冲突检测、撤销和中断事务提示。
- 合成单元测试、真实 ExifTool/Nikon NEF 契约测试和真实样本只读烟测。

### 已知限制

- 首版默认面向个人本机使用，尚未配置 Developer ID 签名、公证和自动更新。
- 首版使用 macOS 系统 Perl 运行随应用分发的 ExifTool；未来 macOS 若移除系统 Perl，需要更换自包含运行时。
- 真实样本中的停留候选不会自动写入，必须在界面中逐项或批量确认。
- 不执行反向地理编码，不写入地址、方向或推测速度。
